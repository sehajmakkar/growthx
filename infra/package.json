{
  "name": "@growthx/infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "deploy": "cdk deploy --require-approval never --outputs-file cdk-outputs.json",
    "diff": "cdk diff",
    "destroy": "cdk destroy --force"
  },
  "dependencies": {
    "@growthx/api": "workspace:*",
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "aws-cdk": "^2.170.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
